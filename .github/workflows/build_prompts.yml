name: Build JSON Database

on:
  push:
    branches: ["main"]
    paths:
      - "data/prompts.yaml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install pyyaml

      - name: Build JSON with numeric_id
        run: |
          python -c "
          import yaml
          import json

          with open('data/prompts.yaml', 'r', encoding='utf-8') as f:
              prompts = yaml.safe_load(f) or []

          def has_foreign_letters(text):
              if not isinstance(text, str):
                  return False
              # Блокируем ЛЮБУЮ букву, которая не является английской (не-ASCII)
              return any(ch.isalpha() and not ch.isascii() for ch in text)

          print('=' * 60)
          print('Processing prompts - skipping non-English entries...')
          print('=' * 60)

          skipped_count = 0
          output_prompts = []

          for i, prompt in enumerate(prompts, start=1):
              fields = {k: prompt.get(k, '') for k in ('title', 'description', 'prompt')}
              bad = [k for k, v in fields.items() if has_foreign_letters(v)]

              if bad:
                  print(f\"Skipping prompt #{i}: non-English letters in: {', '.join(bad)}\")
                  skipped_count += 1
                  continue

              ordered = {'numeric_id': len(output_prompts) + 1}
              ordered.update(prompt)
              output_prompts.append(ordered)

          print('=' * 60)
          print(f'Processed: {len(output_prompts)} included, {skipped_count} skipped.')
          print('=' * 60)

          with open('data/prompts.json', 'w', encoding='utf-8') as f:
              json.dump(output_prompts, f, ensure_ascii=False, indent=2, separators=(', ', ': '))

          print(f'Successfully built data/prompts.json with {len(output_prompts)} prompts.')
          "

      - name: Update prompt count in all READMEs
        run: |
          python -c "
          import json
          import re
          import os

          with open('data/prompts.json', 'r', encoding='utf-8') as f:
              prompts = json.load(f)
          count = len(prompts)
          formatted_count = f'{count:,}'
          print(f'Total prompts: {formatted_count}')

          readme_files = [
              'README.md', 'README_DE.md', 'README_ES.md', 'README_FR.md',
              'README_HI.md', 'README_IT.md', 'README_JA.md', 'README_KO.md',
              'README_PT.md', 'README_RU.md', 'README_ZH.md',
          ]

          pattern = re.compile(
              r'(https://img\.shields\.io/badge/[^-]+-)'
              r'[0-9][0-9%C.,]*'
              r'(-blue)'
          )

          for readme_file in readme_files:
              filepath = os.path.join('.', readme_file)
              if not os.path.exists(filepath):
                  print(f'  SKIP: {readme_file} not found')
                  continue
              with open(filepath, 'r', encoding='utf-8') as f:
                  content = f.read()
              url_count = formatted_count.replace(',', '%2C')
              new_content = pattern.sub(r'\g<1>' + url_count + r'\g<2>', content)
              if new_content != content:
                  with open(filepath, 'w', encoding='utf-8') as f:
                      f.write(new_content)
                  print(f'  UPDATED: {readme_file}')
              else:
                  print(f'  NO CHANGE: {readme_file}')
          "

      - name: Commit and Push JSON
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/prompts.json data/prompts.yaml README.md README_*.md
          if git diff --staged --quiet; then
            exit 0
          fi
          git commit -m "auto: generated prompts.json & updated prompt counts"
          git push